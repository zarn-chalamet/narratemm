package com.narratemm.repository;

import com.narratemm.entity.ScriptSegment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ScriptSegmentRepository extends JpaRepository<ScriptSegment, String> {
}
